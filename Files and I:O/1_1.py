"""
***************************************
**** 5.1. Reading and Writing Text Data
***************************************

#########
# Problem
#########
    - You need to read or write text data,
      possibly in different text encodings such as ASCII, UTF-8, or UTF-16.

#########
# Solution
#########
    - Use the `open()` function with mode `rt` to read a text file.
    - Use `open()` with mode `wt` to write a text file.

# NOTE:
    - Opening file using `with` auto closes file,
      if it was not used, do not forget to `close()`

# TODO:
    - Read the entire file as a single string.
    - Iterate over the lines of the file.
    - Write chunks of text data.
    - Try to open file.txt in `ascii` mode and read
    - File `file.txt` contains line `Spicy Jalapeño`, which contains non `ascii` char
      try to read it by replacing or ingnoring error causing char.
"""

from collections import Iterable


if __name__ == '__main__':
    pass










